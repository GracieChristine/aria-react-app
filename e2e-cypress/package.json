{
  "name": "e2e-cypress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cypress run --browser chrome",
    "test:smoke": "cypress run --browser chrome --env grep=@smoke,grepFilterSpecs=true",
    "open": "cypress open"
  },
  "devDependencies": {
    "@bahmutov/cy-grep": "^2.1.0",
    "cypress": "^14.0.0"
  }
}
